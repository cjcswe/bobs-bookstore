AWSTemplateFormatVersion: '2010-09-09'
Description: 'ATX .NET Application Deployment for ECS with Fargate'

Parameters:
  ApplicationName:
    Type: String
    Description: Name of the application
    {% if main_executable_binary %}Default: {{ main_executable_binary }}{% endif %}

  VpcId:
    Type: String
    Description: VPC ID where the application will be deployed
    {% if ecs_vpc_id %}Default: {{ ecs_vpc_id }}{% endif %}

  PrivateSubnetIds:
    Type: String
    Description: Private subnet IDs where ECS tasks will run
    {% if subnet_ids %}Default: {{ ecs_cluster_subnet_ids }}{% endif %}

  AlbListenerArn:
    Type: String
    Description: ARN of the ALB listener for routing
    {% if ecs_alb_arn %}Default: {{ ecs_alb_arn }}{% endif %}

  EcsClusterName:
    Type: String
    Description: Name of the ECS cluster to deploy to
    {% if ecs_cluster_name %}Default: {{ ecs_cluster_name }}{% endif %}

  EcsSecurityGroupId:
    Type: String
    Description: Security group ID for ECS tasks
    {% if security_group_id %}Default: {{ security_group_id }}{% endif %}

  ListenerRulePriority:
    Type: Number
    Description: Priority for the ALB listener rule
    {% if ecs_alb_listener_rule_priority %}Default: {{ ecs_alb_listener_rule_priority }}{% endif %}

  ContainerImageUri:
    Type: String
    Description: URI of existing container image to deploy.
    {% if image_uri %}Default: {{ image_uri }}{% endif %}

  Cpu:
    Type: Number
    Description: CPU units for the ECS task
    Default: {{ cpu }}
    AllowedValues: [256, 512, 1024, 2048, 4096]

  Memory:
    Type: Number
    Description: Memory (MB) for the ECS task
    Default: {{ memory }}
    AllowedValues: [512, 1024, 2048, 4096, 8192, 16384]

  OperatingSystem:
    Type: String
    Description: Container operating system
    Default: '{{ os }}'
    AllowedValues: [Linux]

  ContainerPort:
    Type: Number
    Description: The container port to expose
    Default: {{ container_port }}
    MinValue: 1
    MaxValue: 65535

  EnvironmentVariables:
    Type: String
    Description: Key-value pairs for container environment variables
    Default: '{{ env_vars | tojson }}'

  HostHeader:
    Type: String
    Description: Hostname to match in ALB listener rule for host-based routing
    Default: '{% if host_header %}{{ host_header }}{% endif %}'

  CloudWatchLogGroup:
    Type: String
    Description: Log group and stream configuration in format 'LogGroup,StreamPrefix'
    Default: '{{ cw_log_group }}'

  CloudWatchLogStreamPrefix:
    Type: String
    Description: Log group and stream configuration in format 'LogGroup,StreamPrefix'
    Default: '{{ cw_log_stream_prefix }}'

  TaskCount:
    Type: Number
    Description: Number of tasks ECS will keep active
    Default: {{ ecs_task_count }}
    MinValue: 1

  HealthCheckPath:
    Type: String
    Description: Path for target group health checks
    Default: '/health'

Conditions:
  UseEmptyEnvironment: !Equals [!Ref EnvironmentVariables, '{}']
  UseHostBasedRouting: !Not [!Equals [!Ref HostHeader, '']]
  UseCwLogConfiguration: !Not [!Equals [!Ref CloudWatchLogGroup, '']]

Resources:
  LogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !If 
        - UseCwLogConfiguration
        - !Select CloudWatchLogGroup
        - !Sub /ecs/${ApplicationName}
      RetentionInDays: 14
      Tags:
        - Key: ResourceName
          Value: !Sub ${ApplicationName}-logs
        - Key: CreatedBy
          Value: AWSTransform
        - Key: TransformationType
          Value: DotNET

  TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      HealthCheckPath: !Ref HealthCheckPath
      Name: !Sub ${ApplicationName}-tg
      Port: !Ref ContainerPort
      Protocol: HTTP
      TargetType: ip
      VpcId: !Ref VpcId
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 5
      Tags:
        - Key: ResourceName
          Value: !Sub ${ApplicationName}-tg
        - Key: CreatedBy
          Value: AWSTransform
        - Key: TransformationType
          Value: DotNET

  ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      Actions:
        - Type: forward
          TargetGroupArn: !Ref TargetGroup
      Conditions:
        - Field: host-header
          HostHeaderConfig:
            Values: 
              - !Ref HostHeader
      ListenerArn: !Ref AlbListenerArn
      Priority: !Ref ListenerRulePriority


  TaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Ref ApplicationName
      Cpu: !Ref Cpu
      Memory: !Ref Memory
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      ExecutionRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ApplicationName}-ExecutionRole
      TaskRoleArn: !Sub arn:aws:iam::${AWS::AccountId}:role/${ApplicationName}-TaskRole
      RuntimePlatform:
        OperatingSystemFamily: !Ref OperatingSystem
      ContainerDefinitions:
        - Name: !Ref ApplicationName
          Image: !Ref ContainerImageUri
          Essential: true
          PortMappings:
            - ContainerPort: !Ref ContainerPort
          Environment: !If 
            - UseEmptyEnvironment
            - !Ref AWS::NoValue
            - !Split [',', !Ref EnvironmentVariables]
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !If 
                - UseCwLogConfiguration
                - !Select [0, !Split [",", !Ref CwLogConfiguration]]
                - !Sub /ecs/${ApplicationName}
              awslogs-stream-prefix: !If 
                - UseCwLogConfiguration
                - !Select [1, !Split [",", !Ref CwLogConfiguration]]
                - ecs
              awslogs-region: !Ref AWS::Region
      Tags:
        - Key: ResourceName
          Value: !Sub ${ApplicationName}-td
        - Key: CreatedBy
          Value: AWSTransform
        - Key: TransformationType
          Value: DotNET

  Service:
    Type: AWS::ECS::Service
    DependsOn: ListenerRule
    Properties:
      ServiceName: !Ref ApplicationName
      Cluster: !Ref EcsClusterName
      TaskDefinition: !Ref TaskDefinition
      DesiredCount: !Ref TaskCount
      LaunchType: FARGATE
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: DISABLED
          Subnets: !Split [',', !Ref PrivateSubnetIds]
          SecurityGroups:
            - !Ref EcsSecurityGroupId
      LoadBalancers:
        - ContainerName: !Ref ApplicationName
          ContainerPort: !Ref ContainerPort
          TargetGroupArn: !Ref TargetGroup
      Tags:
        - Key: ResourceName
          Value: !Sub ${ApplicationName}-service
        - Key: CreatedBy
          Value: AWSTransform
        - Key: TransformationType
          Value: DotNET

  ServiceAutoScalingTarget:
    Type: AWS::ApplicationAutoScaling::ScalableTarget
    DependsOn: Service
    Properties:
      MaxCapacity: 10
      MinCapacity: 1
      ResourceId: !Join 
        - '/'
        - - service
          - !Ref EcsClusterName
          - !Ref ApplicationName
      RoleARN: !Sub arn:aws:iam::${AWS::AccountId}:role/aws-service-role/ecs.application-autoscaling.amazonaws.com/AWSServiceRoleForApplicationAutoScaling_ECSService
      ScalableDimension: ecs:service:DesiredCount
      ServiceNamespace: ecs

  ServiceAutoScalingPolicy:
    Type: AWS::ApplicationAutoScaling::ScalingPolicy
    Properties:
      PolicyName: !Sub ${ApplicationName}-target-tracking
      PolicyType: TargetTrackingScaling
      ScalingTargetId: !Ref ServiceAutoScalingTarget
      TargetTrackingScalingPolicyConfiguration:
        PredefinedMetricSpecification:
          PredefinedMetricType: ECSServiceAverageCPUUtilization
        TargetValue: 70

Outputs:
  ServiceName:
    Description: ECS Service Name
    Value: !Ref Service

  TaskDefinitionArn:
    Description: Task Definition ARN
    Value: !Ref TaskDefinition

  TargetGroupArn:
    Description: Target Group ARN
    Value: !Ref TargetGroup

  LogGroupName:
    Description: CloudWatch Log Group Name
    Value: !Ref LogGroup